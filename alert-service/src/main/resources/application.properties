spring.application.name=alert-service
server.port=${ALERT_SERVICE_PORT:8088}
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/alertdb}

# ── Email (SOC incident notifications) ──────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:risktrace001@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:yzwh xayn wvzi agqg}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# SOC notification recipient — change to real SOC inbox in production
app.soc-email=${SOC_EMAIL:risktrace001@gmail.com}